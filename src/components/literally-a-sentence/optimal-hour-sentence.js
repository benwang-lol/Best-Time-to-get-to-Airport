import React from 'react';

const OptimalHour = (props) => {
    return (
        <div>
            <h1>Optimal Number of Hours to Arrive Early at the Airport: {props.oH}</h1>
        </div>
    );
}

export default OptimalHour;